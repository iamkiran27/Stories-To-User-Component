import React from 'react'

interface props {
    value  : string 
    setsearchedValue : React.Dispatch<React.SetStateAction<string>>;
}

const SearchWithIcon = (props : props) => {
  return (
    <div>

    </div>
  )
}

export default SearchWithIcon