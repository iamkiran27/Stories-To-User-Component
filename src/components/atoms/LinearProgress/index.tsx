import React from "react";

interface ProgressValue {
  value?: number;
}

const LinearProgress = (props: ProgressValue) => {
  return (
    <div>
      {/* use MUI LinearProgress component ans render according by the value prop */}
    </div>
  );
};

export default LinearProgress;
